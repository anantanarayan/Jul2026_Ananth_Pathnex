i learnt so many git scenarios

git config --global user.name "name"
git config --global user.email "email"

git remote add origin"https"

git add . -- to remove git reset filename.ext
git stautus
git commit -m " add3ed my first"

git push -u origin main 
this is for the first time 
----------------------------
scenarios
=========
i accidently deleted the file which was there in in original , i want to again get back the
those files , 
git reset .  --> this brings back , but doesnt unstage the changes that i recently created files.

The "Nuclear" Reset (Safest to restore exact server files)
git fetch origin -- if committed , this removes the local changes 

Reset your local branch to match the server:
git reset --hard origin/main
Remove any extra, untracked files you created locally (like new scratch files)
git clean -fd


similarly if i want the file but later 
Method 2: The "Keep a Backup" ApproachIf you want a fresh set of files 
but are worried you might need your current local changes later, stash them in a safe holding area:Hide your current changes in a safe Git holding area:
>> git stash
Use code with caution.Pull down the fresh files from the server:
>>> git pull
Use code with caution.If you decide you definitively do not want the stashed changes anymore, you can empty the holding area:
>> git stash drop

